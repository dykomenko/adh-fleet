# Секреты — никогда в git
.env
*.pass

# Бэкапы и мусор
*.tar.gz
*.bak

# Шаблон конфига коммитится намеренно: секретов в нём нет.
# Адреса и bcrypt-хеш пароля заменены плейсхолдерами,
# подстановка происходит на ноде во время установки.
# Проверка — scripts/make-template.sh откажется писать шаблон с хешем.
